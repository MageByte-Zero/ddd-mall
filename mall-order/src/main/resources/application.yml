server:
  port: 8084
  servlet:
    context-path: /

spring:
  application:
    name: mall-order
  profiles:
    active: dev
  datasource:
    url: jdbc:mysql://localhost:3306/mall_order?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: ddd_root_2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        # L4 不用 Nacos 配置中心；第 4 讲骨架阶段只用本地 application.yml
        enabled: false
        server-addr: localhost:8848
        file-extension: yaml
        import-check:
          enabled: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: AUTO
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

seata:
  # L4 暂不启用 Seata；第 10 讲引入 AT 模式时改为 true
  enabled: false
  application-id: mall-order
  tx-service-group: ddd-mall-tx-group
  registry:
    type: nacos
    nacos:
      server-addr: localhost:8848
      namespace: ""
      group: SEATA_GROUP
  config:
    type: nacos
    nacos:
      server-addr: localhost:8848
      group: SEATA_GROUP

rocketmq:
  name-server: localhost:9876
  producer:
    group: mall-order-producer
    sendMessageTimeout: 10000
